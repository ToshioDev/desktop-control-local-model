# Artifacts Directory

Expected future subdirectories:

- `checkpoints/` for offline model checkpoints.
- `exports/` for serialized offline model exports.
- `reports/` for evaluation outputs.

TODO: Keep generated outputs reproducible and avoid storing secrets or credentials.
