# Offline training configuration stub.
# TODO: Point this at a validated dataset once schemas are finalized.

training:
  enabled: false
  dataset_split: train
  batch_size: 8
  epochs: 1
  device: cpu
