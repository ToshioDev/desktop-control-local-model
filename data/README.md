# Data Directory

Expected future subdirectories:

- `raw/` for manually collected and still-unreviewed inputs.
- `processed/` for validated, normalized datasets.

TODO: Add dataset manifests and validation tooling before storing training data here.
